import {
    Button,
    ButtonGroup,
    Row,
    Col,
    Container
} from "react-bootstrap";
export default function ButtonOpen() {
    return (
        <div >
            <div class="col-*-*">
                <ButtonGroup aria-label="Basic example">
                    <Button variant="secondary">Open Lid</Button>
                    <Button variant="secondary">Close Lid</Button>
                    <Button variant="secondary">Send to the Gril</Button>
                </ButtonGroup>
            </div>
            <div class="col-*-*">
                <ButtonGroup aria-label="Basic example">
                    <Button variant="secondary">Open Lid</Button>
                    <Button variant="secondary">Close Lid</Button>
                    <Button variant="secondary">Send to the Gril</Button>
                </ButtonGroup> </div>
            <div class="col-*-*">
                <ButtonGroup aria-label="Basic example">
                    <Button variant="secondary">Open Lid</Button>
                    <Button variant="secondary">Close Lid</Button>
                    <Button variant="secondary">Send to the Gril</Button>
                </ButtonGroup> </div>
        </div>


    );
}
